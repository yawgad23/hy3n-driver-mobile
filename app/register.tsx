// The complete five-step application flow lives here so every entry point
// requires vehicle details and remains pending until an administrator approves it.
export { default } from './driver/register';
